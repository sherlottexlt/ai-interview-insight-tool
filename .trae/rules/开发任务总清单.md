---
alwaysApply: true
---
请按以下顺序协助我完成项目开发，每一步完成后等待我确认再继续：

1. 创建项目目录结构。
2. 生成 requirements.txt、.env.example、.gitignore、config.py。
3. 生成 schemas.py，用 Pydantic 定义数据结构。
4. 生成 prompts.py，包含单份访谈分析和多访谈聚类 Prompt。
5. 生成 llm_client.py，封装 DeepSeek API 调用。
6. 生成 parser.py，处理 JSON 提取和 Schema 校验。
7. 生成 scoring.py，实现 RICE 优先级评分。
8. 生成 visualization.py，实现 Plotly 图表。
9. 生成 report_generator.py，实现 Markdown 报告导出。
10. 生成 app.py，实现 Streamlit 页面。
11. 生成 5 份 sample_data 访谈样本。
12. 协助我本地运行和修复报错。
13. 优化页面 UI 和交互。
14. 生成 README.md。
15. 给出适合写进 AI 产品经理作品集的项目总结。

请严格遵守：
- 不要一次性生成所有文件。
- 每次只完成一个阶段。
- 代码要可运行。
- API Key 从 .env 读取。
- 输出 JSON 必须可解析。
- 每个痛点必须带用户原话 evidence。